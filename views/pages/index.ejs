<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

  <h1 class="team-signup__header-logo" title="<%= app.name %>">
    <%- include(app.logo); %>
    <span class="team-signup__header-logo-text"><%- app.header_logo_text || app.name %></span>
  </h1>

  <div class="team-signup__content">

  <% if (user.is_admin) { %>
    <h2>Admin Tools:</h2>
    <a href="/api/admin/reset" data-api-action>Reset Teams &gt;</a>
  <% } %>

  <% if (user.full_name || user.is_admin) { %>

    <% if (!user.is_admin) { %>
      <h3>Hi, <%= user.full_name %>!</h3>
      <a href="/api/setname?full_name=" data-api-action>Edit My Name</a>
    <%  } %>

    <% if (app.config.devMode) { %>
      <p><em>my guid: <%= user.guid %></em></p>
    <% } %>

    <h2>Choose your team:</h2>

    <ol class="team-signup__team-listing">
        <% teams.forEach(function(team){ %>
          <li data-team-id="<%= team.id %>">
            <%- include('../partials/team.ejs', { team }); %>
          </li>
        <% }); %>
    </ol>
  <% } else { %>
    <form action="/api/setname" method="GET" data-form-name="enter-name">
      <input name="full_name" placeholder="Enter your full name please." class="team-signup__name-entry" >
      <input type="submit" value="Submit" />
    </form>
  <% } %>

  </div>

  <% if (app.config.devMode) { %>
    <script src="<%= app.config.browserRefreshURL %>"></script>
  <% } %>

  <script src="/assets/js/app.js"></script>
</body>
</html>
